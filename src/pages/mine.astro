---
import Comment from "@components/comment/index.astro";
import { PageHeader } from "@components/features/page-header";
import Markdown from "@components/misc/Markdown.astro";
import MainGridLayout from "@layouts/MainGridLayout.astro";
import { getEntry, render } from "astro:content";

import I18nKey from "../i18n/i18nKey";
import { i18n } from "../i18n/translation";

export const prerender = true;

const entry = await getEntry("spec", "mine");
const specRendered = entry ? await render(entry) : null;
const SpecMarkdown = specRendered?.Content;
const title = i18n(I18nKey.navMy);
---

<MainGridLayout title={title} description={title}>
	<script>
		import("../scripts/right-sidebar-layout.js");
	</script>
	<div
		class="flex w-full rounded-[var(--radius-large)] overflow-hidden relative min-h-32"
	>
		<div class="card-base z-10 px-9 py-6 relative w-full">
			<PageHeader title={title} />
			<Markdown class="mt-2 prose dark:prose-invert max-w-none">
				{
					SpecMarkdown ? (
						<SpecMarkdown />
					) : (
						<p>
							在内容仓库的 <code>spec/</code> 下添加{" "}
							<code>mine.md</code> 后即可显示正文。
						</p>
					)
				}
			</Markdown>
		</div>
	</div>
	<div class="mt-4">
		<Comment path="/mine/" />
	</div>
</MainGridLayout>
